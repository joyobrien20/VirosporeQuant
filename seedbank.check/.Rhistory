geom_line() + # Line plot for each sample # Optional: Points for each time point
labs(title = "",
x = "Time (hours)",
y = "OD 600") +  # Clean and minimal theme
theme(legend.title = element_blank())  # Remove legend title
ggplot(OD_long, aes(x = time_clean, y = OD600)) +
geom_line() +
facet_wrap(~ Sample) +
theme_minimal() +
scale_y_continuous(limits=c(0, 0.5))
View(OD_long)
library(tidyr)
library(dplyr)
#OD_adjusted <- OD_adjusted %>%
#select (-Time)
# Reshape the data from wide to long format
OD_long <- pivot_longer(OD_adjusted,
cols = -time_clean,  # Keep the "Time" column as is
names_to = "Sample",  # Name for the new column with sample names
values_to = "OD600")  # Name for the fluorescence readings
library(ggplot2)
# Create the plot
ggplot(OD_long, aes(x = time_clean, y = OD600, color = Sample)) +
geom_line() + # Line plot for each sample # Optional: Points for each time point
labs(title = "",
x = "Time (hours)",
y = "OD 600") +  # Clean and minimal theme
theme(legend.title = element_blank())  # Remove legend title
ggplot(OD_long, aes(x = time_clean, y = OD600)) +
geom_line() +
facet_wrap(~ Sample) +
theme_minimal() +
scale_y_continuous(limits=c(0, 0.3))
View(OD_long)
# Create the plot
ggplot(OD_long, aes(x = time_clean, y = OD600, color = Sample)) +
geom_line() + # Line plot for each sample # Optional: Points for each time point
labs(title = "",
x = "Time (hours)",
y = "OD 600") #+  # Clean and minimal theme
str(OD_long)
as.numeric(OD_long$time_clean)
View(OD_long)
library(tidyr)
library(dplyr)
#OD_adjusted <- OD_adjusted %>%
#select (-Time)
# Reshape the data from wide to long format
OD_long <- pivot_longer(OD_adjusted,
cols = -time_clean,  # Keep the "Time" column as is
names_to = "Sample",  # Name for the new column with sample names
values_to = "OD600")  # Name for the fluorescence readings
# Get rid of the extra rows
OD_long <- OD_long[!is.na(OD_long$OD600)]
library(tidyr)
library(dplyr)
#OD_adjusted <- OD_adjusted %>%
#select (-Time)
# Reshape the data from wide to long format
OD_long <- pivot_longer(OD_adjusted,
cols = -time_clean,  # Keep the "Time" column as is
names_to = "Sample",  # Name for the new column with sample names
values_to = "OD600")  # Name for the fluorescence readings
# Get rid of the extra rows
OD_long <- OD_long[!is.na(OD_long$OD600), ]
library(ggplot2)
# Create the plot
ggplot(OD_long, aes(x = time_clean, y = OD600, color = Sample)) +
geom_line() + # Line plot for each sample # Optional: Points for each time point
labs(title = "",
x = "Time (hours)",
y = "OD 600") #+  # Clean and minimal theme
#theme(legend.title = element_blank())  # Remove legend title
#ggplot(OD_long, aes(x = time_clean, y = OD600)) +
#geom_line() +
#facet_wrap(~ Sample) +
#theme_minimal() +
#scale_y_continuous(limits=c(0, 0.3))
View(OD_long)
head(OD_long)
library(tidyr)
library(dplyr)
#OD_adjusted <- OD_adjusted %>%
#select (-Time)
# Reshape the data from wide to long format
OD_long <- pivot_longer(OD_adjusted,
cols = -time_clean,  # Keep the "Time" column as is
names_to = "Sample",  # Name for the new column with sample names
values_to = "OD600")  # Name for the fluorescence readings
# Get rid of the extra rows
OD_long <- OD_long[!is.na(OD_long$OD600), ]
library(ggplot2)
# adjust time
library(ggplot2)
OD_long <- OD_long %>%
mutate(
time_hours =
as.numeric(substr(time_clean, 1, 2)) +
as.numeric(substr(time_clean, 4, 5)) / 60 +
as.numeric(substr(time_clean, 7, 8)) / 3600
)
# Create the plot
ggplot(OD_long, aes(x = time_clean, y = OD600, color = Sample)) +
geom_line() + # Line plot for each sample # Optional: Points for each time point
labs(title = "",
x = "Time (hours)",
y = "OD 600") #+  # Clean and minimal theme
#theme(legend.title = element_blank())  # Remove legend title
#ggplot(OD_long, aes(x = time_clean, y = OD600)) +
#geom_line() +
#facet_wrap(~ Sample) +
#theme_minimal() +
#scale_y_continuous(limits=c(0, 0.3))
library(tidyr)
library(dplyr)
#OD_adjusted <- OD_adjusted %>%
#select (-Time)
# Reshape the data from wide to long format
OD_long <- pivot_longer(OD_adjusted,
cols = -time_clean,  # Keep the "Time" column as is
names_to = "Sample",  # Name for the new column with sample names
values_to = "OD600")  # Name for the fluorescence readings
# Get rid of the extra rows
OD_long <- OD_long[!is.na(OD_long$OD600), ]
library(ggplot2)
# adjust time
library(ggplot2)
OD_long <- OD_long %>%
mutate(
time_hours =
as.numeric(substr(time_clean, 1, 2)) +
as.numeric(substr(time_clean, 4, 5)) / 60 +
as.numeric(substr(time_clean, 7, 8)) / 3600
)
# Create the plot
ggplot(OD_long, aes(x = time_clean, y = OD600, color = Sample)) +
geom_smooth() + # Line plot for each sample # Optional: Points for each time point
labs(title = "",
x = "Time (hours)",
y = "OD 600") #+  # Clean and minimal theme
#theme(legend.title = element_blank())  # Remove legend title
#ggplot(OD_long, aes(x = time_clean, y = OD600)) +
#geom_line() +
#facet_wrap(~ Sample) +
#theme_minimal() +
#scale_y_continuous(limits=c(0, 0.3))
library(tidyr)
library(dplyr)
#OD_adjusted <- OD_adjusted %>%
#select (-Time)
# Reshape the data from wide to long format
OD_long <- pivot_longer(OD_adjusted,
cols = -time_clean,  # Keep the "Time" column as is
names_to = "Sample",  # Name for the new column with sample names
values_to = "OD600")  # Name for the fluorescence readings
# Get rid of the extra rows
OD_long <- OD_long[!is.na(OD_long$OD600), ]
library(ggplot2)
# adjust time
library(ggplot2)
OD_long <- OD_long %>%
mutate(
time_hours =
as.numeric(substr(time_clean, 1, 2)) +
as.numeric(substr(time_clean, 4, 5)) / 60 +
as.numeric(substr(time_clean, 7, 8)) / 3600
)
# Create the plot
ggplot(OD_long, aes(x = time_clean, y = OD600, color = Sample)) +
geom_line() + # Line plot for each sample # Optional: Points for each time point
labs(title = "",
x = "Time (hours)",
y = "OD 600") #+  # Clean and minimal theme
#theme(legend.title = element_blank())  # Remove legend title
#ggplot(OD_long, aes(x = time_clean, y = OD600)) +
#geom_line() +
#facet_wrap(~ Sample) +
#theme_minimal() +
#scale_y_continuous(limits=c(0, 0.3))
View(OD_long)
View(OD_long)
library(tidyr)
library(dplyr)
#OD_adjusted <- OD_adjusted %>%
#select (-Time)
# Reshape the data from wide to long format
OD_long <- pivot_longer(OD_adjusted,
cols = -time_clean,  # Keep the "Time" column as is
names_to = "Sample",  # Name for the new column with sample names
values_to = "OD600")  # Name for the fluorescence readings
# Get rid of the extra rows
OD_long <- OD_long[!is.na(OD_long$OD600), ]
library(ggplot2)
# adjust time
library(ggplot2)
OD_long <- OD_long %>%
mutate(
time_hours =
as.numeric(substr(time_clean, 1, 2)) +
as.numeric(substr(time_clean, 4, 5)) / 60 +
as.numeric(substr(time_clean, 7, 8)) / 3600
)
# Create the plot
ggplot(OD_long, aes(x = time_ours, y = OD600, color = Sample)) +
geom_line() + # Line plot for each sample # Optional: Points for each time point
labs(title = "",
x = "Time (hours)",
y = "OD 600") #+  # Clean and minimal theme
# Create the plot
ggplot(OD_long, aes(x = time_hours, y = OD600, color = Sample)) +
geom_line() + # Line plot for each sample # Optional: Points for each time point
labs(title = "",
x = "Time (hours)",
y = "OD 600") #+  # Clean and minimal theme
#theme(legend.title = element_blank())  # Remove legend title
#ggplot(OD_long, aes(x = time_clean, y = OD600)) +
#geom_line() +
#facet_wrap(~ Sample) +
#theme_minimal() +
#scale_y_continuous(limits=c(0, 0.3))
```
library(tidyr)
library(dplyr)
#OD_adjusted <- OD_adjusted %>%
#select (-Time)
# Reshape the data from wide to long format
OD_long <- pivot_longer(OD_adjusted,
cols = -time_clean,  # Keep the "Time" column as is
names_to = "Sample",  # Name for the new column with sample names
values_to = "OD600")  # Name for the fluorescence readings
# Get rid of the extra rows
OD_long <- OD_long[!is.na(OD_long$OD600), ]
library(ggplot2)
# adjust time
library(ggplot2)
OD_long <- OD_long %>%
mutate(
time_hours =
as.numeric(substr(time_clean, 1, 2)) +
as.numeric(substr(time_clean, 4, 5)) / 60 +
as.numeric(substr(time_clean, 7, 8)) / 3600
)
# Create the plot
ggplot(OD_long, aes(x = time_hours, y = OD600, color = Sample)) +
geom_smooth() + # Line plot for each sample # Optional: Points for each time point
labs(title = "",
x = "Time (hours)",
y = "OD 600") #+  # Clean and minimal theme
#theme(legend.title = element_blank())  # Remove legend title
#ggplot(OD_long, aes(x = time_clean, y = OD600)) +
#geom_line() +
#facet_wrap(~ Sample) +
#theme_minimal() +
#scale_y_continuous(limits=c(0, 0.3))
# setwd
set.wd("~/GitHub/VirosporeQuant/seedbank.check")
# setwd
setwd("~/GitHub/VirosporeQuant/seedbank.check")
library(paletteer)
library(ggplot2)
theme_set(theme_classic() +
theme(axis.title = element_text(size = 16),
axis.title.x = element_text(margin = margin(t = 15, b = 15)),
axis.title.y = element_text(margin = margin(l = 15, r = 15)),
axis.text = element_text(size = 13),
axis.text.x = element_text(margin = margin(t = 5)),
axis.text.y = element_text(margin = margin(r = 5)),
#axis.line.x = element_line(linewidth = 1),
#axis.line.y = element_line(linewidth = 1),
axis.ticks.y.right = element_line(linewidth = 1),
axis.text.y.right = element_text(size = 13, margin = margin(l = 5)),
axis.line.y.right = element_line(linewidth = 0.1),
axis.ticks.x.top = element_line(linewidth = 1),
axis.text.x.top = element_text(size = 13, margin = margin(b = 5)),
axis.line.x.top = element_line(linewidth = 0.1),
axis.line.x = element_blank(),
axis.line.y = element_blank(),
axis.ticks.x = element_line(linewidth = 1),
axis.ticks.y = element_line(linewidth = 1),
axis.ticks.length = unit(.1, "in"),
panel.border = element_rect(color = "grey20", fill = NA, linewidth = 1.5),
legend.text = element_text(size = 14),
strip.text = element_text(size = 14),
strip.background = element_blank()
))
colors <-paletteer_d("ggthemes::Miller_Stone")
WLO.l1 <- read.txt("~/GitHub/VirosporeQuant/seedbank.check/Data/WLO_L1_phage_long_seed_bank_SPO1_filtered_phage_annotated_timecourse.txt")
WLO.l1 <- read.table("~/GitHub/VirosporeQuant/seedbank.check/Data/WLO_L1_phage_long_seed_bank_SPO1_filtered_phage_annotated_timecourse.txt")
WLO.l1 <- read.table("~/GitHub/VirosporeQuant/seedbank.check/Data/WLO_L1_phage_long_seed_bank_SPO1_filtered_phage_annotated_timecourse.txt", header = TRUE)
WLO.l1 <- read.table("~/GitHub/VirosporeQuant/seedbank.check/Data/WLO_L1_phage_long_seed_bank_SPO1_filtered_phage_annotated_timecourse.txt", header = TRUE)
WLO.l1 <- read.table("~/GitHub/VirosporeQuant/seedbank.check/Data/WLO_L1_phage_long_seed_bank_SPO1_filtered_phage_annotated_timecourse.txt", header = TRUE, sep = "\t", fill = TRUE, quote = "")
View(WLO.l1)
head(WLO.l1)
WLO.l1 <- read.csv("~/GitHub/VirosporeQuant/seedbank.check/Data/WLO_L1_phage_long_seed_bank_SPO1_filtered_phage_annotated_timecourse.txt", header = TRUE)
View(WLO.l1)
WLO.l1 <- read.csv("~/GitHub/VirosporeQuant/seedbank.check/Data/WLO_L1_phage_long_seed_bank_SPO1_filtered_phage_annotated_timecourse.txt", header = TRUE)
WLO.l2 <- read.csv("~/GitHub/VirosporeQuant/seedbank.check/Data/WLO_L2_phage_long_seed_bank_SPO1_filtered_phage_annotated_timecourse.txt", header = TRUE)
WLO.l3 <- read.csv("~/GitHub/VirosporeQuant/seedbank.check/Data/WLO_L3_phage_long_seed_bank_SPO1_filtered_phage_annotated_timecourse.txt", header = TRUE)
SNO.l1 <- read.csv("~/GitHub/VirosporeQuant/seedbank.check/Data/SNO_L1_phage_no_seed_bank_SPO1_filtered_phage_annotated_timecourse.txt", header = TRUE)
WLO.l1 <- read.csv("~/GitHub/VirosporeQuant/seedbank.check/Data/WLO_L1_phage_long_seed_bank_SPO1_filtered_phage_annotated_timecourse.txt", header = TRUE)
WLO.l2 <- read.csv("~/GitHub/VirosporeQuant/seedbank.check/Data/WLO_L2_phage_long_seed_bank_SPO1_filtered_phage_annotated_timecourse.txt", header = TRUE)
WLO.l3 <- read.csv("~/GitHub/VirosporeQuant/seedbank.check/Data/WLO_L3_phage_long_seed_bank_SPO1_filtered_phage_annotated_timecourse.txt", header = TRUE)
SNO.l1 <- read.csv("~/GitHub/VirosporeQuant/seedbank.check/Data/SNO_L1_phage_no_seed_bank_SPO1_filtered_phage_annotated_timecourse.txt", header = TRUE)
SNO.l2 <- read.csv("~/GitHub/VirosporeQuant/seedbank.check/Data/SNO_L2_phage_no_seed_bank_SPO1_filtered_phage_annotated_timecourse.txt", header = TRUE)
SNO.l3 <- read.csv("~/GitHub/VirosporeQuant/seedbank.check/Data/SNO_L3_phage_no_seed_bank_SPO1_filtered_phage_annotated_timecourse.txt", header = TRUE)
View(WLO.l3)
View(WLO.l3)
View(SNO.l1)
View(SNO.l1)
head(SNO.11)
head(SNO.l1)
View(WLO.l3)
View(SNO.l1)
View(WLO.l1)
View(SNO.l1)
View(WLO.l1)
View(WLO.l2)
View(WLO.l3)
View(SNO.l1)
View(SNO.l2)
library(dplyr)
library(tidyr)
library(ggplot2)
WLO.l1$Dataset <- "Spore.lysate1"
WLO.l2$Dataset <- "Spore.lysate2"
WLO.l3$Dataset <- "Spore.lysate3"
SNO.l1$Dataset <- "Nonspore.lysate1"
SNO.l2$Dataset <- "Nonspore.lysate2"
SNO.l3$Dataset <- "Nonspore.lysate3"
all_mut <- bind_rows(WLO.l1, WLO.l2, WLO.l3, SNO.l1, SNO.l2, SNO.l3)
View(all_mut)
library(dplyr)
library(tidyr)
library(ggplot2)
WLO.l1$Dataset <- "Spore.lysate1"
WLO.l2$Dataset <- "Spore.lysate2"
WLO.l3$Dataset <- "Spore.lysate3"
SNO.l1$Dataset <- "Nonspore.lysate1"
SNO.l2$Dataset <- "Nonspore.lysate2"
SNO.l3$Dataset <- "Nonspore.lysate3"
all_mut <- bind_rows(WLO.l1, WLO.l2, WLO.l3, SNO.l1, SNO.l2, SNO.l3)
all_mut_long <- all_mut %>%
pivot_longer(
cols = starts_with("Freq."),
names_to = "Timepoint",
values_to = "Frequency"
)
View(all_mut_long)
View(all_mut)
View(all_mut_long)
View(all_mut)
ggplot(all_mut_long, aes(x = Position, y = Frequency, color = Dataset)) +
geom_point(alpha = 0.6, size = 1.5) +
facet_wrap(~Timepoint, ncol = 1) +
theme_bw()
View(all_mut_long)
View(all_mut_long)
View(all_mut)
View(all_mut)
library(dplyr)
library(tidyr)
library(ggplot2)
WLO.l1$Dataset <- "Spore.lysate1"
WLO.l2$Dataset <- "Spore.lysate2"
WLO.l3$Dataset <- "Spore.lysate3"
SNO.l1$Dataset <- "Nonspore.lysate1"
SNO.l2$Dataset <- "Nonspore.lysate2"
SNO.l3$Dataset <- "Nonspore.lysate3"
spore.mut <- bind_rows(WLO.l1, WLO.l2, WLO.l3)
nonspore.mut <- bind_rows(SNO.l1, SNO.l2, SNO.l3)
#all_mut <- bind_rows(WLO.l1, WLO.l2, WLO.l3, SNO.l1, SNO.l2, SNO.l3)
#all_mut_long <- all_mut %>%
# pivot_longer(
#   cols = starts_with("Freq."),
#   names_to = "Timepoint",
#   values_to = "Frequency"
# )
library(dplyr)
library(tidyr)
library(ggplot2)
WLO.l1$Dataset <- "Spore.lysate1"
WLO.l2$Dataset <- "Spore.lysate2"
WLO.l3$Dataset <- "Spore.lysate3"
SNO.l1$Dataset <- "Nonspore.lysate1"
SNO.l2$Dataset <- "Nonspore.lysate2"
SNO.l3$Dataset <- "Nonspore.lysate3"
spore.mut <- bind_rows(WLO.l1, WLO.l2, WLO.l3)
nonspore.mut <- bind_rows(SNO.l1, SNO.l2, SNO.l3)
# Add labels for treatment
spore.mut$Coevolve <- "Spore"
nonspore.mut$Coevolve <- "Non-spore"
#all_mut <- bind_rows(WLO.l1, WLO.l2, WLO.l3, SNO.l1, SNO.l2, SNO.l3)
#all_mut_long <- all_mut %>%
# pivot_longer(
#   cols = starts_with("Freq."),
#   names_to = "Timepoint",
#   values_to = "Frequency"
# )
all.mut <- bindrows(spore.mut, nonspore.mut)
library(dplyr)
library(tidyr)
library(ggplot2)
WLO.l1$Dataset <- "Spore.lysate1"
WLO.l2$Dataset <- "Spore.lysate2"
WLO.l3$Dataset <- "Spore.lysate3"
SNO.l1$Dataset <- "Nonspore.lysate1"
SNO.l2$Dataset <- "Nonspore.lysate2"
SNO.l3$Dataset <- "Nonspore.lysate3"
spore.mut <- bind_rows(WLO.l1, WLO.l2, WLO.l3)
nonspore.mut <- bind_rows(SNO.l1, SNO.l2, SNO.l3)
# Add labels for treatment
spore.mut$Coevolve <- "Spore"
nonspore.mut$Coevolve <- "Non-spore"
all.mut <- bindrows(spore.mut, nonspore.mut)
all.mut <- bind_rows(spore.mut, nonspore.mut)
View(all_mut)
View(all.mut)
library(dplyr)
library(tidyr)
library(ggplot2)
WLO.l1$Dataset <- "Spore.lysate1"
WLO.l2$Dataset <- "Spore.lysate2"
WLO.l3$Dataset <- "Spore.lysate3"
SNO.l1$Dataset <- "Nonspore.lysate1"
SNO.l2$Dataset <- "Nonspore.lysate2"
SNO.l3$Dataset <- "Nonspore.lysate3"
spore.mut <- bind_rows(WLO.l1, WLO.l2, WLO.l3)
nonspore.mut <- bind_rows(SNO.l1, SNO.l2, SNO.l3)
# Add labels for treatment
spore.mut$Coevolve <- "Spore"
nonspore.mut$Coevolve <- "Non-spore"
all.mut <- bind_rows(spore.mut, nonspore.mut)
# Reshape frequencies
long.mut <- all.mut %>%
pivot_longer(
cols = starts_with("Freq."),
names_to = "Timepoint",
values_to = "Frequency"
)
#all_mut <- bind_rows(WLO.l1, WLO.l2, WLO.l3, SNO.l1, SNO.l2, SNO.l3)
#all_mut_long <- all_mut %>%
# pivot_longer(
#   cols = starts_with("Freq."),
#   names_to = "Timepoint",
#   values_to = "Frequency"
# )
View(long.mut)
View(long.mut)
wide.mut <- long.mut %>%
select(Position, Gene, Timepoint, Treatment, Frequency) %>%
pivot_wider(
names_from = Treatment,
values_from = Frequency
)
wide.mut <- long.mut %>%
select(Position, Gene, Timepoint, Coevolve, Frequency) %>%
pivot_wider(
names_from = Treatment,
values_from = Frequency
)
wide.mut <- long.mut %>%
select(Position, Gene, Timepoint, Coevolve, Frequency) %>%
pivot_wider(
names_from = Coevolve,
values_from = Frequency
)
View(wide.mut)
long.mut <- all.mut %>%
pivot_longer(
cols = starts_with("Freq."),
names_to = "Timepoint",
values_to = "Frequency"
)
# Alignn mutations between treatments
wide.mut <- long.mut %>%
select(Position, Gene, Timepoint, Coevolve, Frequency) %>%
pivot_wider(
names_from = Coevolve,
values_from = Frequency
)
View(long.mut)
library(dplyr)
library(tidyr)
library(ggplot2)
WLO.l1$Dataset <- "Spore.lysate1"
WLO.l2$Dataset <- "Spore.lysate2"
WLO.l3$Dataset <- "Spore.lysate3"
SNO.l1$Dataset <- "Nonspore.lysate1"
SNO.l2$Dataset <- "Nonspore.lysate2"
SNO.l3$Dataset <- "Nonspore.lysate3"
spore.mut <- bind_rows(WLO.l1, WLO.l2, WLO.l3)
nonspore.mut <- bind_rows(SNO.l1, SNO.l2, SNO.l3)
# Add labels for treatment
spore.mut$Coevolve <- "Spore"
nonspore.mut$Coevolve <- "Non-spore"
all.mut <- bind_rows(spore.mut, nonspore.mut)
# Reshape frequencies
long.mut <- all.mut %>%
pivot_longer(
cols = starts_with("Freq."),
names_to = "Timepoint",
values_to = "Frequency"
)
# Align mutations between treatments
wide.mut <- long.mut %>%
select(Position, Gene, Allele, Mutation.type, Timepoint, Coevolve, Frequency) %>%
pivot_wider(
names_from = Coevolve,
values_from = Frequency
)
View(wide.mut)
View(long.mut)
